// Generated by Skyramp v1.3.30 on 2026-06-30 00:12:36.19034426 +0000 UTC m=+237.439554261
// Command: skyramp generate contract rest http://localhost:3000/client-config \
// 		--framework playwright \
// 		--language typescript \
// 		--method GET \
// 		--output-dir /home/runner/work/sut-eval-twenty/sut-eval-twenty/tests/skyramp/twenty-server-backend \
// 		--overwrite \
// 		--provider \
// 		--response-status-code 200 \
// 		--runtime local

// Import of required libraries
import { test, expect, SkyrampClient, getBaseUrl, getValue } from '@skyramp/skyramp';

// URL for test requests
const URL = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:3000");

// contract test for /client-config GET
test('testClientConfigGet', async () => {
    // Invocation of Skyramp Client
    const client = new SkyrampClient();
    // Definition of authentication header
    const headers: Record<string, string> = {};
    if (process.env.SKYRAMP_TEST_TOKEN && process.env.SKYRAMP_TEST_TOKEN != "") {
        headers["Authorization"] = "Bearer " + process.env.SKYRAMP_TEST_TOKEN;
    }

    // Execute Request
    const clientConfigGetResponse = await client.sendRequest({
        url: URL,
        path: "/client-config",
        method: "GET",
        headers: headers
    });

    // Generated Assertions
    expect(clientConfigGetResponse.statusCode, 'status code').toBe(200);
    expect(getValue(clientConfigGetResponse, 'authProviders'), 'authProviders').not.toBeUndefined();
    expect(getValue(clientConfigGetResponse, 'edition'), 'edition').toBeNull();
});
